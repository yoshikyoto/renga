<!-- unfollow -->
<% # remote:true によって非同期通信(Ajax)を使用する %>

<%= form_for(current_user.relationships.find_by(followed_id: @user),
    html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn btn-large" %>
<% end %>
