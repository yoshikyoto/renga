<% provide(:title, @user.name) %>

<div class="row">
  <aside class="span4">
    <section>
      <h1>
	<%= gravatar_for @user %>
	<%= @user.name %>
      </h1>
      <h2>
	<%= link_to "プロフィール編集", edit_user_path(current_user) if current_user && @user = current_user %>
      </h2>
    </section>
  </aside>
</div>

<% @user.email %>
